# Plan: Additional UI/UX Improvements

## Requirements

1. **Report page - Add prisoner numbers** in the discipline section (เบิกตัวผู้ต้องขัง) - show sequential numbers
2. **Improve Eventlog view** - make it scrollable in a box, show only 100 entries max, make it look beautiful
3. **Fix Vinai user** - cannot see รอตรวจสอบวินัย status data (likely demo data uses old 'รอตรวจสอบ' status)
4. **Tadtel approval flow** - two options: approve all visitors at once OR approve per individual
5. **Refactor table action buttons** - make them look neat and clean, prevent text overlap/wrap
6. **Show printer name (displayName)** in all reports - currently shows in print view but should show in report listing
7. **Hide seedDefaultUser and demo data** - remove from production or conditionally show

## Implementation Plan

### 1. Fix Vinai Status Filtering (admin.js)
- The demo data uses old status `รอตรวจสอบ` which should be normalized to `รอตรวจสอบวินัย`
- Verify normalizeStatus function handles this correctly (line 1196-1204)
- The demo data on line 178 should work since normalizeStatus maps it

### 2. Add Prisoner Numbers in Report Discipline Section (admin.js lines 2563-2567)
- Change the discipline section output to include sequential numbering:
```javascript
html += `<div class="dept" style="border-color:#c62828;">`;
html += `<strong style="color:#c62828">🚨 ส่วนทัณฑ์ (เบิกตัวผู้ต้องขัง)</strong>`;
html += prisoners.map((p, i) => `${i+1}. ${p}`).join('<br>');
html += `</div>`;
```

### 3. Improve Eventlog View (admin.html + admin.js)
- Wrap eventlog in a scrollable container with max-height
- Limit display to 100 entries max
- Add proper styling for the log container
- Update renderEventlog function

### 4. Tadtel Approval Flow (admin.js viewDetail section)
- Add a toggle/button to switch between "approve all" and "individual approve" modes
- When approving from detail view, allow Tadtel to either:
  - Approve all visitors automatically (one click)
  - Or manually approve each visitor individually via the approval buttons

### 5. Refactor Table Action Buttons (admin.css + admin.js)
- Add flex-wrap: nowrap and min-width constraints
- Ensure buttons don't wrap or overlap
- Use consistent button width or icon-only approach for tight spaces

### 6. Show Printer DisplayName in Reports (admin.js)
- Update all report print functions to show `currentUser.displayName` prominently
- Include in printReport, printPrisonerVinaiList, printDailyDeptReports

### 7. Hide seedDefaultUser and Demo Data
- In `google_apps_script_updated.js`: Remove or comment out the `seedDefaultUsers` call (line 738) or add a flag to prevent seeding in production
- In `src/js/admin.js`: The demo data is only shown when `APPS_SCRIPT_URL` is not configured (line 162) - this is already conditional. Add a flag to disable demo mode:
  - Add check: `if (window.DEMO_MODE !== false && !APPS_SCRIPT_URL)` before calling getDemoData
- Alternatively, rename demo user entries or remove them entirely for production

## Files to Modify

- `src/js/admin.js`: Main logic changes for all features
- `src/css/admin.css`: Styling improvements for eventlog and action buttons
- `admin.html`: Structure changes for eventlog container
- `google_apps_script_updated.js`: Remove or disable default user seeding