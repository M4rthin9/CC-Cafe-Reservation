# ระบบจองเยี่ยมผู้ต้องขัง — สรุปการเปลี่ยนแปลง

## 📋 อัปเดต: ขั้นตอนการชำระเงิน

### สถานะการจองที่เปลี่ยน

**เดิม:** รอตรวจสอบ → อนุมัติแล้ว → ชำระแล้ว

**ใหม่:** รอตรวจสอบ → รอชำระเงิน → ชำระแล้ว → เสร็จสิ้น

---

## 1️⃣ หน้าเจ้าหน้าที่ (prison_staff_dashboard.html)

### ✅ เพิ่ม: ปุ่ม "ปฏิเสธการชำระเงิน"

เมื่อสถานะเป็น **"ชำระแล้ว"** (ญาติได้อัปโหลดสลิปแล้ว) จะแสดง 2 ปุ่ม:

- **💳 ยืนยันชำระเงิน** → เปลี่ยนสถานะเป็น `เสร็จสิ้น`
- **✗ ปฏิเสธ** → กลับไปสถานะ `รอชำระเงิน` (ญาติต้องทำใหม่)

**โค้ด:**
```javascript
${isPaid ? `
  <button class="btn-confirm-pay" onclick="confirmPayment(${rowIdx})">💳 ยืนยันชำระเงิน</button>
  <button class="btn-reject-pay" onclick="rejectPayment(${rowIdx})">✗ ปฏิเสธ</button>
` : ''}
```

### ✅ อัปเดต: สถานะบนแดชบอร์ด

- **รอตรวจสอบ** = `badge-wait` (สีเหลือง)
- **รอชำระเงิน** = `badge-pay` (สีน้ำเงินอ่อน) ← NEW
- **ชำระแล้ว** = `badge-paid` (สีฟ้า) ← NEW
- **เสร็จสิ้น** = `badge-done` (สีเขียว) ← NEW
- **ไม่อนุมัติ** = `badge-reject` (สีแดง)

### ✅ แก้ไข: รูปสลิปมาถึงแดชบอร์ด

**ปัญหา:** ในเดิม Google Apps Script ซ่อนข้อมูล `slipImage` ในการดึงข้อมูล
```javascript
// OLD - ซ่อนไป
if (h !== 'slipImage') obj[h] = row[i];
```

**แก้ไข:** รวมข้อมูล `slipImage` ให้เสมอ
```javascript
// NEW - รวมทุกคอลัมน์
obj[h] = row[i];
```

### ✅ ปรับปรุง: การแสดงรูปสลิป

**ปุ่ม "🧾 สลิป"** ตอนนี้แสดง:
- ✅ **มีรูป** → แสดงรูปสลิป + ข้อมูลการจอง
- ⏳ **ชำระแล้ว แต่ยังไม่มีรูป** → แจ้งว่า "ยังไม่มีรูป" พร้อมข้อมูล
- 📋 **ยังไม่ชำระ** → แจ้งว่า "รอการชำระเงิน"

---

## 2️⃣ หน้าตรวจสอบสถานะ (status.html)

### ✅ อัปเดต: ไป showPayment เมื่อสถานะ "รอชำระเงิน"

**เดิม:**
```javascript
if (status === 'อนุมัติ' || status === 'approved') {
  // แสดงฟอร์มชำระเงิน
}
```

**ใหม่:**
```javascript
if (status === 'รอชำระเงิน' || status === 'อนุมัติ' || status === 'approved') {
  // แสดงฟอร์มชำระเงิน
}
```

### ✅ อัปเดต: Status Pills

| สถานะ | แสดง |
|---|---|
| `รอชำระเงิน` | `✅ อนุมัติ — รอชำระเงิน` |
| `ชำระแล้ว` | `💳 รอเจ้าหน้าที่ยืนยัน` |
| `เสร็จสิ้น` | `✅ เสร็จสิ้นแล้ว` |
| `อนุมัติ` | `✅ อนุมัติแล้ว` |
| `ไม่อนุมัติ` | `❌ ไม่อนุมัติ` |
| `รอตรวจสอบ` | `⏳ รอตรวจสอบ` |

### ✅ ปรับปรุง: ข้อความแจ้งเตือน

- **เมื่อชำระแล้ว:** "รอเจ้าหน้าที่ยืนยัน" (ไม่ใช่ "ชำระแล้ว")
- **เมื่อเสร็จสิ้น:** "การจองเสร็จสมบูรณ์"

### ✅ ปรับปรุง: ฟอร์มอัปโหลดสลิป

- แสดงชื่อไฟล์ + ขนาด
- Preview รูปก่อนส่ง
- ข้อมูลชัดเจนว่าสลิปจะถูกส่งไปยังแดชบอร์ด

---

## 3️⃣ Google Apps Script (google_apps_script_updated.js)

### ✅ แก้ไข: รวมข้อมูล slipImage

```javascript
// ส่วน doGet('getAll') - ตอนนี้รวมรูปสลิป
const rows = data.slice(1).map(row => {
  const obj = {};
  headers.forEach((h, i) => {
    // Include all data including slipImage
    obj[h] = row[i];
  });
  return obj;
});
```

---

## 🔄 ขั้นตอนการจองใหม่

```
1. ญาติ: กรอกข้อมูล → เลขอ้างอิง (Ref)
         ↓
2. เจ้าหน้าที่: ตรวจสอบประวัติวินัย 1-2 วัน
         ↓
3. เจ้าหน้าที่ dashboard: กด "✓ อนุมัติ"
         ↓
   สถานะ: รอตรวจสอบ → รอชำระเงิน
         ↓
4. ญาติ: ตรวจสอบสถานะ → เห็น "ดำเนินการชำระเงิน"
         ↓
5. ญาติ: โอนเงิน → อัปโหลดสลิป
         ↓
   สถานะ: รอชำระเงิน → ชำระแล้ว
         ↓
6. เจ้าหน้าที่ dashboard: ดู "🧾 สลิป" ยืนยันได้
         ↓
7. เจ้าหน้าที่: กด "💳 ยืนยันชำระเงิน"
         ↓
   สถานะ: ชำระแล้ว → เสร็จสิ้น
         ↓
8. ญาติ: ตรวจสอบสถานะ → เห็น "เสร็จสิ้นแล้ว"
```

---

## 📁 ไฟล์ที่อัปเดต

1. ✅ `prison_staff_dashboard.html` - เพิ่มปุ่มปฏิเสธ + แสดงรูปสลิป
2. ✅ `status.html` - แสดงฟอร์มชำระเงินสำหรับ "รอชำระเงิน"
3. ✅ `google_apps_script_updated.js` - รวม slipImage ในข้อมูลดึง

---

## ⚠️ สิ่งที่ต้องทำ

### ก่อนใช้งาน

1. **อัปเดต Google Apps Script**
   - อัปเดตโค้ดจาก `google_apps_script_updated.js`
   - Deploy ใหม่ (Publish → New deployment)

2. **เช็ค Google Sheet**
   - คอลัมน์ต้องมี: `ref`, `status`, `slipImage`
   - ถ้าไม่มีให้เพิ่มคอลัมน์ `slipImage` ด้วย

---

## 🧪 ทดสอบ

### ทดสอบหน้า Staff Dashboard
```
1. เข้าระบบด้วย password: 10900
2. กด "✓ อนุมัติ" → สถานะเปลี่ยนเป็น "รอชำระเงิน" ✓
3. ถ้าเจ้าหน้าที่ demo อัปโหลดรูป → status.html โอเค
   - ให้สถานะเปลี่ยนเป็น "ชำระแล้ว" ด้วย
4. กด "💳 ยืนยันชำระเงิน" → status "เสร็จสิ้น" ✓
5. กด "🧾 สลิป" → ต้องเห็นรูป ✓
```

### ทดสอบหน้า Status Check
```
1. ค้นหาการจองที่มี status "รอชำระเงิน"
2. ต้องเห็นปุ่ม "ดำเนินการชำระเงิน" ✓
3. อัปโหลดรูป → ต้องเห็น preview ✓
4. กด "ยืนยันการชำระเงิน" → status เปลี่ยน ✓
```

---

**อัปเดต:** 21 พฤษภาคม 2026
